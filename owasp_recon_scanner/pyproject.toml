[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "owasp-recon-scanner"
version = "0.1.0"
description = "Authorized, low-impact web reconnaissance and OWASP control checks"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
owasp-recon = "owasp_recon.cli:main"

[tool.setuptools.packages.find]
where = ["."]
